%------------------------------------------------------------------------
function r = degenfn_line2d(X)
% 计算X中行向量的距离,X的每一列代表一个数据点
        r =  any(pdist(X) < eps);
        % pdist函数计算X的行向量之间的距离
end